
앞으로 닥쳐올 상황을 
부정적 결과를 기본값으로 두고 대비를 하면
50의 데미지를 받아도 50밖에 데미지가 안된다 라고 본다.
긍정적 결과를 기본값으로 두면 50이나 데미지가 들어온다는 관점이된다.

미션전 부정적 기본값을 갖자



스태틱은 프로그램이 시작될때 메모리에 할당됩니다. - 실행이 안되었는데도 할당이 되어버렷!
전역처럼 보이지만 전역은 아닙니다.
메모리에 항상 할당되기때문에 전략적으로 사용할게 아니라면 사용하면 낭비적이긴하다.


같은 디렉토리 내에  #include ./ex_range.c
하여 외부에 있는 놈을 쓰려면  extern 이라는 키워드를 쓰면 외부 파일의 변수를 쓸수 있다. - 사실 안써도 불러오면
b로 접근 가능! ㅋㅋ

랜덤의 진실 ?? - 계산된 결과일뿐이다.


void srand(time_ seed)
{
    r =seed;
}


#include <time.h>
#include <stdlib.h>

srand(time(NULL));
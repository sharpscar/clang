#include <stdio.h>

//배열을 만들어서 모든 오더들을 넣자
void calc_daily()
{
    // pr->total 값만 꺼내서 배열에 넣자
    int daily_total[1000] = {0,};
    int total=0;
    // 
    int index_order=0; // 메인함수에 오더주문을 카운트 하는 장치를 여기 넣음

    //각오더를 전부 반복하여 1차원 배열에 때려넣자
    // for(int i=0; i<index_order; i++)
    // {
    //     daily_total[i] = pr->total //이런식으로?
    // }

    return daily_total;

}

int main()
{

        calc_daily();


    return 0;
}